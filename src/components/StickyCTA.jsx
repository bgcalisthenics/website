import React from 'react';

const StickyCTA = () => (
  <div className="sticky-cta-bar">
    <span>Ready to transform? </span>
    <a href="#programs" className="sticky-cta-btn">Get Started Now</a>
  </div>
);

export default StickyCTA;
